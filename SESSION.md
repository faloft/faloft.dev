# SESSION log — portfolio

## 2026-04-24 — Шаг 1

Создана структура проекта:

```
PETPROJ/portfolio/
├── CLAUDE.md
├── SESSION.md
├── cases/
├── positioning/
└── public/
```

В `CLAUDE.md` зафиксированы: источники, формат write-up, границы, NDA-рамка, эмоциональная рамка.

**Следующий шаг:** Шаг 2 — write-up кейса "Справочники" в `cases/case_01_school_directory.md`.

Источники для кейса:

- `C:\Round\Оптимизация процессов\Справочники\Алгоритм_очистки_справочника_v10.docx`
- `Отчёт_по_очистке.xlsx`, `Справочник_чистый_v18.xlsx`, `Справочник_удалённых_v18.xlsx`

## 2026-04-25 — Шаг 2 ✅

Write-up кейса "Справочники" готов: `cases/case_01_school_directory.md` (~600 слов).

Ключевые факты, зафиксированные в кейсе:
- **50 280 → 41 980 строк** (~17% мусора, ~8 300 нецелевых записей).
- Скрипт `clean_v10.py` (pandas + regex), 8 шагов; финальная версия — v18, принята РП.
- 18 итераций с РП через файл удалённых с колонкой "Причина".

Что встроено по дополнениям Фаниса:
- **Формализация правил** как первая и самостоятельная ценность (от ручной чистки "по чувству" к явным правилам).
- **Главный урок:** не "выгрузить всё → чистить", а "уточнить ЦА → собирать сразу нужное". Мартышкин труд vs целевой алгоритм.
- **Эволюция процесса:** один промпт → потерял контекст → файлы → сессии → проект Claude. Переход случился именно на этом кейсе.
- **Личный слой:** ручная чистка по чувству невоспроизводима даже у одного человека; моё "по чувству" расходилось бы с РП и со мной завтрашним → формализация была единственным способом сдвинуться.

Решения по NDA:
- Заказчик ROUND не назван.
- РП обезличена.
- Скрипт не публикуется ("доступен под NDA по запросу").

**Следующий шаг:** Шаг 3 — write-up кейса "Content Creator" в `cases/case_02_content_creator.md`.

Источники:
- `C:\Round\Оптимизация процессов\Content Creator\01_Project\project_passport_v3_4.md`
- `product_decisions_sprint6.md`, `project_instructions_v2.md`
- `02_Agent_Logic/` (5 промптов агентов)
- `03_Test_Runs/` (прогоны с командой)

Акцент — PM (видение продукта, решения, итерации архитектуры).

## 2026-04-25 (s2) — Шаг 3 ⚠️ в работе

Первый драфт `cases/case_02_content_creator.md` написан, признан Фанисом неудачным. План переработки: `C:\Users\Fanis\.claude\plans\quirky-riding-rabbit.md`.

## 2026-04-26 — Шаг 3, итерация 2 ⚠️ ждёт ревью

Кейс переписан целиком по плану `C:\Users\Fanis\.claude\plans\02-mighty-rain.md`. Структура: Проблема → Видение → Что сделал (3 хода: цепочка промптов в чате → Skills как MVP в Claude.ai → Claude Code как несостоявшийся финал) → Бэклог → Результат → Выводы.

Прошёл круг правок по фидбеку Фаниса (по абзацам, не вычитан целиком):

- Убран жаргон «Модуль 2 vs Модуль 1» — переформулировано человечно.
- Переписан блок **навигатор + триггер «Готово»**: теперь как ответ на UX-боль РП-новичков (использовали нейронки как гугл, копипасты, переименования файлов). Навигатор ведёт прогон, собирает выходы 5 агентов в единый контекст и обратную связь — в файлы output и feedback. «Готово» — сигнал агенту-чату упаковать артефакт в md. **В первой версии я переврал смысл «Готово» — приписал ему борьбу с «преждевременным артефактом».**
- **Ход 3** дополнен честно: тестирование v2 с РП не закрыто (последняя неделя стажировки команда переходила на удалёнку из-за финансовых трудностей, не до прогонов) + лимиты бесплатной Claude.ai + личный страх Фаниса перед Skills и Claude Code как реальный фактор.
- Строка «6 спринтов / паспорт v3.4 / логика v1.7» из Результата убрана, заменена формулировкой в Ходе 2: «полный список продуктовых решений — в отдельном decisions log» (вариант (б) на Q1, под вопросом).
- **Выводы переписаны на язык Фаниса** (5 пунктов): сразу проверять с пользователями; возможно Claude Code/Skills тут не нужны; в следующий раз — от примеров-эталонов, а не от архитектуры; сессионная работа в чатах = экономия токенов; интересная работа крутится в голове сама.

**Фанис правки не прочитал — ушёл со словами «пока», ничего не читал.**

**Следующий шаг (начало следующей сессии):** Фанис читает текущую версию `cases/case_02_content_creator.md`. Дальше:

- Если зашло — пометить шаг 3 ✅, перейти к шагу 4 (`positioning/about.md` из `C:\Round\AI_Present\` + `SELF/ABOUTME.md` раздел 4).
- Если что-то не зашло — точечная правка по фидбеку.

Открытый вопрос Q1: формулировка «полный список — в отдельном decisions log» — оставить или убрать упоминание совсем (читателю-чужому это может быть тёмная лошадка).

NDA: ROUND назван, заказчик — нет, артефакты «под NDA по запросу».

## 2026-04-26 (s2) — Шаги 2 и 3 ✅, переупаковка под продажу

Запрос Фаниса: «месяц прошёл — лидов нет, переупакуй как продажник, у которого задача упаковать и продать». План: `C:\Users\Fanis\.claude\plans\federated-painting-gadget.md`.

Диагноз через шапку маркетолога: оба кейса отвечали не на тот вопрос. Жанр был «дневник стажёра» (как я мыслил, что делал, что не успел). Покупатель кейса сканирует за 10 секунд и ищет «что я получу». Сменили жанр.

**case_02_content_creator** — переписан целиком (~210 слов вместо ~600):

- Убран блок «Видение Claude Code» и Ход 3 «несостоявшийся» (с тремя причинами: удалёнка / лимиты / страх) — целиком. v2 = финал того, что сделано.
- Убраны 5 буллетов «Выводы» и «Снятие ложного ограничения #6».
- Структура «Что сделал» переформатирована из нарратива трёх ходов в 3 буллета результата (v1, v2, прогоны → системный риск).
- Слоган-принцип в конце: **«AI предлагает, человек решает. AI не источник фактов.»**
- output/feedback переформулировано точно (output = результаты от мини-справки до задания; feedback = обратная связь РП по каждому прогону).

**case_01_school_directory** — подрезан в том же ключе (~330 слов вместо ~700):

- Цифра в заголовок: «Справочник школ из 50 280 строк».
- Список целевых/нецелевых сжат, технические детали 8 шагов скрипта — в одну строку.
- «Эволюция процесса работы внутри кейса» (промпт → файлы → сессии → проект Claude) убрана — это рефлексия стажёра, не продажа.
- Слоган-принцип: **«Сначала формализовать правила, потом писать код. ИИ — соавтор правил, а не чистильщик.»**

Оба кейса теперь в одном скан-формате: заголовок с pain → проблема → результат буллетами → почему работает → принцип-слоган → NDA-подвал.

Фанис: «давай оставим, иначе я никогда не пойду дальше» — решение принято осознанно, не из перфекционизма.

**Следующий шаг:** Шаг 4 — `positioning/about.md`. Источники: `C:\Round\AI_Present\visual_brief_review.md` (слайды 1, 3, 5, 9, 14) + `SELF/ABOUTME.md` раздел 4 («как со мной работать»).

## 2026-04-26 (s3) — Шаг 4 ✅

`positioning/about.md` написан (~150 слов). Структура и интонация совпадают с подрезанными кейсами:

- Первый абзац — кто я, что делаю, откуда (один год ROUND + кейсы, до этого event/ops).
- «Как работаю с AI» — рабочая модель из воркшопа (`AI_Present/visual_brief_review.md` слайды 1, 3, 5, 9, 14): черновик/рамка, контекст важнее формулировки, дирижёр-оркестр, слоган «AI предлагает, человек решает. AI не источник фактов.» (тот же что в case_02 — связь между кейсами и позиционированием).
- «Что даю» — 3 буллета ценности: PM-мышление, AI-архитектура, работа от пользователя.
- CTA — оба кейса + почта.

Что сознательно не вошло: «не лекция / не магия / не эксперт» из слайдов воркшопа — на сцене работает, на лендинге звучит оборонительно. Лучше показать чем, чем не быть.

Правка Фаниса: убрать упоминание электромонтажного бизнеса. Внесено.

**Следующий шаг:** Шаг 5 — стиль лендинга. Собрать 3–5 референсов, выбрать 1–2 прилагательных (минимал / брутал / софт / техно), палитру (2–3 цвета), шрифт. Выход — `public/style_note.md`. До кода лендинга.

## 2026-04-27 (s5) — BIO.md + правка about.md ✅

Не шаг портфолио, но опорный артефакт для шага 5. План: `C:\Users\Fanis\.claude\plans\warm-painting-frost.md`.

- `SELF/BIO.md` создан (~600 слов): дуга карьеры (event 2011–2022 → саббатикал 2022–2024 → School 21 + ROUND 2025–2026), что даю, куда иду через делание (по Замесину), фильтр для входящих (4 ключевых критерия из `Колесо_критерии.csv`), 2 истории-якоря (снегоход, Кумбель), CTA.
- `positioning/about.md` строка 3: «Последний год — ROUND» → «2026 — ROUND» (факт-чек: стажировка 3 месяца, 19.01–24.04.2026).

Правки Фаниса по ходу:
- Блок «2022–2024» переписан с «откатной истории / скользкости» (внутреннее) на «саббатикал, время для семьи и для себя» — BIO как продающий артефакт, не дневник.
- Блок «куда иду» переписан с перечня ролей на позиционирование через делание: «автоматизируй вокруг себя → кто увидит, тот придёт». Целевые контуры и фильтр вынесены отдельно.
- История про медь («кучка слева–кучка справа») в BIO не пошла — это инсайт самопознания, не продажа. Записана как кандидат на абзац в `SELF/ABOUTME.md` (см. `INBOX/next-session.md`).

**Следующий шаг:** Шаг 5 — стиль лендинга. Источник тона/структуры теперь — `SELF/BIO.md`.

## 2026-04-27 (s6) — запуск лендинга подготовлен, остановились до npm install

Рабочий лог по самому Astro-проекту: `public/SESSION.md`.

Что закрыто:

- Node.js установлен через `winget install OpenJS.NodeJS.LTS`.
- Проверено: `node v24.15.0`, `npm 11.12.1`.
- В Claude Code установлены плагины `frontend-design`, `playwright`, `context7`.
- Добавлен персональный skill `C:\Users\Fanis\.claude\skills\design-producer\SKILL.md`: превращает расплывчатый запрос «нужен стильный современный сайт/презентация» в самобриф → дизайн-направление → производство → проверку.
- `INBOX/next-session.md` обновлён под следующий запуск.

Что НЕ запускали:

- `npm install`
- `npm run dev`

**Следующий шаг:** открыть `PETPROJ/portfolio/public/SESSION.md`, затем:

```powershell
cd c:\AI_Native_Faloft\PETPROJ\portfolio\public
npm install
npm run dev
```

## 2026-04-27 (s7) — Двойной деплой Vercel + Amvera ✅

Лендинг ушёл в публичный мир. План: `C:\Users\Fanis\.claude\plans\eventual-singing-pumpkin.md`.

Живые URL:
- **Vercel**: https://faloft-dev.vercel.app/ — глобальный, дефолт.
- **Amvera**: https://faloft-dev-faloft.amvera.io/ — стабильный доступ из РФ (Vercel в РФ частично заблокирован).

GitHub: https://github.com/faloft/faloft.dev — публичный, описание на английском («Personal site & portfolio: PM, AI integration, ROUND case studies»).

Что сделали:
- **Перенесли git-репо** с уровня `public/` на уровень `portfolio/`. Причина: `content.config.ts` грузит кейсы через `glob({ base: '../cases' })` — без переноса деплой бы упал на пустой коллекции `cases`. История трёх локальных коммитов сброшена, новый initial commit `d8f3a52`.
- **`.gitignore` в корне** — секреты (`.env`, `.key`, `.pem`, `id_rsa*`), `node_modules/`, `dist/`, IDE-мусор. Сознательно решили НЕ выносить git выше (`PETPROJ/` или `AI_Native_Faloft/`) — там `wordflow_bot/.env`, `SELF/`, `JOB/` и `INBOX/` с личным/NDA, риск утечки одной ошибкой `git add`.
- **Установили `gh` CLI** через winget (был не на PATH в текущей сессии, фикс — открыть новый терминал).
- **Vercel**: импорт из GitHub UI, Root Directory = `public`, Astro auto-detect, бесплатный тариф. Заработал с первого деплоя.
- **Amvera** оказался не статическим хостингом, а Node-контейнером. Решение:
  - Корневой `package.json` (wrapper) с скриптами `build` (`cd public && npm install && npm run build`) и `start` (`node server.js`).
  - `server.js` — мини-сервер на 15 строк через `serve-handler` (та же библиотека, что под капотом у Vercel `serve`).
  - `amvera.yml`: nodejs/npm 20, build через `additionalCommands`, run `npm run start`.
- **Два бага по дороге**, оба пофиксили:
  1. `additionalCommands` нельзя как YAML-список — Amvera передавала в bash буквально `[cmd]`. Фикс: строкой.
  2. Контейнер Amvera запускается не от root, порт 80 запрещён (`EACCES`). Фикс: `containerPort: 3000` (внутри), `servicePort: 80` (наружу).
- **Тариф Amvera**: после первого деплоя на «Начальный Плюс» (490₽/мес) переключили на «Пробный» (170₽/мес, 0.1 CPU / 100MB RAM / 2GB SSD) — для статики хватает с запасом.
- **Подключён HTTPS-домен** через Amvera, бесплатный поддомен `*.amvera.io`, SSL автоматический.
- **Проверено с реального iPhone** — оба URL открываются, рендер ок.

Память пользователя:
- В начале сессии Фанис в plan-mode возразил против выноса git вверх по иерархии — обсудили риски (`wordflow_bot/.env` с TG-токеном, личное в `SELF/`), решили оставить на `portfolio/`. Это правильное решение для каждого pet-проекта в общем workspace.

**Следующий шаг:** оставшиеся пункты плана отложены на следующую сессию (см. `INBOX/next-session.md`):
1. Футер-ссылка «→ Исходник» на github.com/faloft/faloft.dev.
2. Lighthouse ≥ 90 (Performance / Accessibility / SEO) в DevTools для обоих URL.
3. Обновление `INBOX/next-session.md` / закрытие.
4. **Образовательная задача от Фаниса:** разобраться в чём разница между нашим деплоем (Astro → Vercel/Amvera) и сайтом на Tilda. Пользователь хочет понять, что мы сделали по сути и зачем это нужно вместо WYSIWYG-конструктора.

## 2026-04-28 (s8) — Спланирован футер + Lighthouse, не выполнен (пользователь ушёл)

План: `C:\Users\Fanis\.claude\plans\synthetic-drifting-squirrel.md`.

Что сделано в сессии:

- Триаж INBOX (3 файла), сверены аудио (21 новая запись Recording 27–47 — отложены, как требует next-session.md).
- Прочитан [public/src/pages/index.astro](public/src/pages/index.astro), точка правки футера зафиксирована (строки 112–114).
- Поднят и обсуждён баг **mailto-кнопки в hero на десктопе**: «Написать →» молча проваливается, если у пользователя не настроен дефолтный mail client (большинство, кто живёт в Gmail/Outlook web). На мобильных работает. Решено отложить, варианты зафиксированы в плане (шаг 5).
- План написан и согласован концептуально, но **не выполнен** — пользователь ушёл до подтверждения через ExitPlanMode.

**Следующий шаг (начало следующей сессии):**

1. Прочитать `INBOX/wrap-2026-04-28.md` и план `synthetic-drifting-squirrel.md`.
2. Создать `INBOX/mailto-cta-issue.md` (содержание — в шаге 5 плана).
3. Подтвердить план и выполнить шаги 1–4: правка футера → commit/push → PageSpeed Insights API для двух URL → отчёт по метрикам Performance / Accessibility / SEO.
4. Дальше — образовательный разбор «Tilda vs наш стек» (главный пункт next-session.md).

## 2026-04-28 (s9) — Футер + Lighthouse выполнены ✅

План: `C:\Users\Fanis\.claude\plans\synthetic-drifting-squirrel.md` (продолжение s8).

Что сделано:

- **Футер** — добавил `· → Исходник` со ссылкой на github.com/faloft/faloft.dev в [public/src/pages/index.astro:113](public/src/pages/index.astro#L113). Глобальный `a { color: var(--accent-1) }` (`global.css:35`) подцепил золотой цвет автоматически, CSS не трогали.
- **PSI API key** — Фанис создал ключ в Google Cloud Console (PageSpeed Insights API enabled, restricted to PSI only). Ключ держится в shell-переменной, в репо не попадает.
- **Lighthouse прогон 1** (mobile, оба URL): Performance 90/90 ✅, Accessibility 81/81 ❌, SEO 100/100 ✅. Три проблемы a11y: контраст тегов кейсов, heading order (h1 → h3 без h2), новая ссылка в футере опирается только на цвет.
- **Правка 1 — контраст**: `--ink-mute #7A7E76` → `#8C8F87` в `global.css:6`. Контраст с `--bg` поднялся с ~4.4:1 до ~5.5:1, проходит WCAG AA. После прогона: a11y 81 → 89.
- **Правка 2 — heading order**: `<p class="section__eyebrow">Кейсы</p>` → `<h2>` в секции `#cases` ([index.astro:57](public/src/pages/index.astro#L57)). В CSS добавил сброс дефолтов h2 — `font-weight: 400; margin: 0 0 1.5rem` в `.section__eyebrow` чтобы не разъехалась типографика.
- **Правка 3 — link in text**: `.footer a { text-decoration: underline; }` в `global.css`. Подчёркивание ссылки «→ Исходник» снимает требование WCAG про link distinguishability.

Финальные метрики (mobile):

| Метрика       | Vercel | Amvera | Цель |
|---------------|--------|--------|------|
| Performance   | 89     | 90     | ≥90  |
| Accessibility | 100    | 100    | ≥90  |
| SEO           | 100    | 100    | ≥90  |

Performance на Vercel показал 89 — шум Lighthouse (метрика плавает ±2 между прогонами), правки на performance не влияли.

**Открытые вопросы / отложено:**

- mailto-баг hero-кнопки на десктопе — зафиксирован в `INBOX/mailto-cta-issue.md`.
- Render-blocking CSS даёт ~2с savings на FCP, но цель ≥90 уже достигнута, не трогаем.
- Amvera-специфика: `serve-handler` шлёт слабые `Cache-Control` (PSI: «Use efficient cache lifetimes»). Микро-импакт.

**Следующий шаг:** образовательный разбор «Tilda vs наш стек» — главный пункт `INBOX/next-session.md`.

## 2026-04-28 (s10) — mailto-CTA + UI-полировка по фидбэкам Фаниса ✅

Продолжение s9 в той же сессии. План mailto-CTA: `C:\Users\Fanis\.claude\plans\dreamy-launching-koala.md`.

Что сделано:

- **mailto-CTA через вариант A** (click-to-copy + tooltip + mailto fallback). Первый JS в проекте — `<script>` блок в `index.astro`, Astro нативно бандлит. Кнопка обёрнута в `.cta-wrap` (`position: relative`), tooltip — `<span class="cta__tooltip mono" role="status" aria-live="polite">`. Стили — `--bg-alt` фон, `--rule` border, fade in/out 0.15s. Локально проверено в Playwright: класс `is-visible` навешивается, через 1.8с снимается, opacity transition работает, скриншот подтвердил визуальное позиционирование.
- **Фидбэки от Фаниса по проду** (3 пункта, прислал скриншоты):
  1. **Фавикон** — был дефолтный «глобус», добавлен `public/public/favicon.svg`: тёмный квадрат `--bg` с скруглением 4px, надпись `fa` lowercase, `f` — `--accent-1` золото, `a` — `--accent-2` зелёный. Шрифт `system-ui` (без зависимости от Inter). Подключён в `Base.astro` через `<link rel="icon" type="image/svg+xml">`.
  2. **Hero — вариант B (УТП-крючок гибрид)**: добавлен `<h2 class="hero__pitch">` между h1 и lede. Текст: «AI-конвейеры из процессов, которые жили в чьей-то голове.» Цвет `--ink` (чисто белый), font-size `clamp(1.4rem, 2.5vw, 1.85rem)`, `font-weight: 500`, `max-width: 24ch`. Уменьшил `.hero__name margin-bottom` с 1.75rem до 0.75rem чтобы pitch «прилип» к имени. **Lede переписан** в три коротких предложения-характеристики: «Воспроизводимые. Не зависят от истории чата. Не ломаются, когда уходит носитель знаний.» — это контент-правка, если не нравится — откатить.
  3. **Футер** «убого выглядел» из-за золотой вспышки + грубого подчёркивания на `→ Исходник`. Стрелка убрана, цвет ссылки изменён с глобального `--accent-1` на `--ink-mute` (`.footer a { color: var(--ink-mute); text-decoration: underline; }`). Теперь ссылка тон-в-тон с остальным футером, отличается только подчёркиванием — WCAG AA «links rely on color» по-прежнему пройден.
- Локальная верификация: `npm run dev`, скриншоты hero и footer, evaluate проверки tooltip/favicon. Всё корректно.

**Открытые вопросы / отложено:**

- Lede переписан без явного запроса (контент-правка). Если Фанису не зайдёт — откатить на предыдущую версию.
- Фавикон: `system-ui` шрифт. На устройствах с очень специфичной типографикой (некоторые Linux DE) может выглядеть иначе. Не критично.
- Render-blocking CSS на performance — по-прежнему ~2с savings, но цель ≥90 достигнута, не трогаем.

**Следующий шаг:** образовательный разбор «Tilda vs наш стек» — главный пункт `INBOX/next-session.md`. Также можно потенциально пройти ещё раз PSI чтобы убедиться, что новые правки a11y не сломали.
